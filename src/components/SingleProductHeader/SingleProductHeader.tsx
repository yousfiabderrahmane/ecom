import styles from "./SingleProductHeader.module.scss";

export const SingleProductHeader = () => {
  return (
    <section className={styles.headerContainer}>
      <div>
        <h1>Home / Products / Category / Title</h1>
      </div>
    </section>
  );
};
